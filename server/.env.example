DATABASE_URL="postgresql://postgres:password@localhost:5432/halyk_hr"
JWT_SECRET="change-me-in-production"
PORT=4000
FRONTEND_URL="http://localhost:3000"

# Email (необязательно — без этих переменных письма не отправляются)
SMTP_HOST="smtp.example.com"
SMTP_PORT=587
SMTP_USER="noreply@halyk.kz"
SMTP_PASS="smtp-password"
